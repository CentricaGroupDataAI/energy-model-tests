version: 2

sources:
  - name: grp_dataeng_dev
    tables:
      - name: churn_holding

models:
  - name: churn_holding_test
    description: "Final churn holding table"
    columns:
      - name: mpxn
        description: "(PK) Reference for this servicepoint usually a external market id like MPAN or MPRN "
        tests:
          - unique
          - not_null