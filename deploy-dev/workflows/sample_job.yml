resources:
  jobs:
    Energy_model_dbt:
      name: Energy_model_dbt
      tags:
        source: dev
        type: SCV-Ingestion
      schedule: ${var.schedule}
      max_concurrent_runs: ${var.max_concurrent_runs}
      git_source: ${bundle.variables.git_source}
      permissions: ${bundle.variables.permissions}
      tasks:
        - task_key: Energy_model_tests
          dbt_task:
            project_directory: ""
            commands:
              - dbt debug --profile databricks_dev
              - dbt deps --profile databricks_dev
            profiles_directory: .dbt
            source: GIT
          existing_cluster_id: ${bundle.target.existing_cluster_id}
      queue:
        enabled: true
